input {
  tcp {
    port  => 5044
    codec => json_lines
    type  => "back"
  }
  tcp {
    port  => 5045
    codec => json_lines
    type  => "front"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "microcrm-%{type}-%{+YYYY.MM.dd}"
  }
}